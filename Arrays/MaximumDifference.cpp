#include<iostream>
#include<climits>
using namespace std;
int maxDifference(int a[], int n)
{
    int maxDiff = a[1] - a[0];
    int result = a[1] - a[0];
    for(int i=1;i<n;i++)
    {
        maxDiff = max(maxDiff, )
        
    }
}